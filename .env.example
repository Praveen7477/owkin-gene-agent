# Copy this file to `.env` and fill in your key to enable the LLM agent.
# Without a key, the app still runs in offline rule-based mode.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the model (defaults to claude-sonnet-5).
# OWKIN_MODEL=claude-haiku-4-5-20251001
